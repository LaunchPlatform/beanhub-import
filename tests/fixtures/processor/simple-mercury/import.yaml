inputs:
- match: "*.csv"
  config:
    extractor: mercury
    prepend_postings:
      - account: Assets:Bank:US:Mercury
        amount:
          number: "{{ amount }}"
          currency: "{{ currency | default('USD', true) }}"

imports:
  - match:
      extractor:
        equals: "mercury"
      desc: "^Amazon (.+)"
    actions:
      - type: add_txn
        file: "output.bean"
        txn:
          narration: "{{ desc }}"
          postings:
            - account: Expenses:FooBar
              amount:
                number: "{{ -amount }}"
                currency: "{{ currency | default('USD', true) }}"
